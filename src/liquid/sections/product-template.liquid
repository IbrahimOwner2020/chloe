<div class="product">
    <section class="product__main">
        <div class="product__images">
            <img class="product__image" src="{{ product.featured_image | img_url: 'master' }}" alt="{{ product.alt }} " class="product__image">
        </div>

        <div class="product__details">
            <div class="product__info">
                <h1 class="product__title">{{ product.title }}</h1>
                <div class="product__price">{{ product.price | money }}</div>
                <div class="product__description">{{ product.description }}</div>
            </div>

            {%- comment -%}
                PRODUCT FORM
            {%- endcomment -%}
            {% form 'product', product %}
                <div class="product__color">
                    <label for="product-color">Color</label>
                    <select name="id" id="product-color">
                        {% for variant in product.variants %}
                            <option value="{{ variant.id }}">{{ variant.title }}</option>
                        {% endfor %}
                    </select>
                </div>
                <button class="product__submit-btn" type="submit">Add To Cart</button>
            {% endform %}
        </div>
    </section>
</div>

